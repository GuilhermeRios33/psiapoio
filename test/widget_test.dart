// Smoke test placeholder — app usa Supabase e não pode ser testado sem mock de inicialização.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () => expect(true, isTrue));
}
